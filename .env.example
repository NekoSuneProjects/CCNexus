PORT=3000
CCNEXUS_PUBLIC_URL=https://ccnexus.example.com

# Optional binary overrides. Docker includes all three.
# YTDLP_BIN=yt-dlp
# FFMPEG_BIN=ffmpeg
# TTS_BIN=espeak-ng

# Database credentials are configured by the first-boot wizard and stored in data/config.json.
