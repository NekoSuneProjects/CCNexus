{
  "name": "ccnexus",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted control plane for CC:Tweaked computers, speakers, turtles and redstone automation.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/store.js && node --check public/app.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^5.1.0",
    "ws": "^8.18.3"
  }
}
