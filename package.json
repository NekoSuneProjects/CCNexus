{
  "name": "ccnexus",
  "version": "0.3.0",
  "private": true,
  "description": "Self-hosted multi-world control plane for CC:Tweaked automation, storage and media.",
  "type": "module",
  "scripts": {
    "start": "node -r dotenv/config src/server.js",
    "dev": "node --watch -r dotenv/config src/server.js",
    "check": "node --check src/server.js && node --check src/store.js && node --check src/database.js && node --check src/toolchain.js && node --check src/youtube-resolver.js && node --check src/tts.js && node --check src/media.js && node --check public/app.js"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "adm-zip": "^0.6.0",
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^13.0.2",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongodb": "^7.5.0",
    "mysql2": "^3.23.2",
    "pg": "^8.22.0",
    "text2wav": "^0.0.14",
    "ws": "^8.18.3"
  }
}
